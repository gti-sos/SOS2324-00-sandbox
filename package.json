{
  "name": "sos2324-00-sandbox",
  "version": "0.0.0",
  "description": "mi primer paquete de node",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "pretest": "npm start &",
    "test-integration": "npx newman run ./tests/integration/api.json -e ./tests/integration/environments/local.json",
    "test-e2e": "npx playwright test",
    "test": "npm run test-integration && npm run test-e2e",
    "posttest": "kill `ps -uax | grep \"node index\" | grep -v \"grep\" | grep -v \"sh\" | awk '{print $2}'`"
  },
  "author": "pablo",
  "license": "GPL-3.0",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cool-ascii-faces": "^1.3.4",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "nedb": "^1.8.0"
  },
  "type": "module",
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "@types/node": "^20.11.30"
  }
}
